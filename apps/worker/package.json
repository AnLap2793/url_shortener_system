{
  "name": "@url-shortener/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": { "@url-shortener/application": "0.0.0", "@url-shortener/db": "0.0.0", "@url-shortener/observability": "0.0.0" },
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "start": "node dist/main.js" }
}
